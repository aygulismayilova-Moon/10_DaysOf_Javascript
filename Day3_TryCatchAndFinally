 * Complete the reverseString function
 * Use console.log() to print to stdout.
 */
function reverseString(s) {
    let reversed_s;
    try {
        reversed_s=s.split("").reverse("").join("");
        console.log(reversed_s);
        //r=s.match(/\d+/g);
        
    } catch (error) {
        //let str="s.pl
        console.log(error.message);
        console.log(s)
        //reversed_s=r
    }

    
}

